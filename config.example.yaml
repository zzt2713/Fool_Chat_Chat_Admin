app:
  addr: 0.0.0.0:9100

database:
  host: 127.0.0.1
  port: 3306
  name: mhkh
  user: root
  password: ""

security:
  # 删除用户等高危操作的二级密码
  delete_password: ""

ai:
  # AI 服务地址，默认 https://api.deepseek.com
  base_url: ""
  # AI API Key（不要提交到 git）
  api_key: ""
  # 模型名，默认 deepseek-chat
  model: ""
